import 'package:flutter/material.dart';

const backgroundColor = Color(0xFF191B27);
const itemColor = Color(0xFF282C3A);

const gradientActiveTermsCategory = LinearGradient(colors: [
  Color(0xFF016AC9),
  Color(0xFF2393F9),
]);
const gradientActiveTermsCategoryTransparrent = LinearGradient(colors: [
  Colors.transparent,
  Colors.transparent,
]);
const gradientButtonGreen = LinearGradient(colors: [
  Color(0xFF14C966),
  Color(0xFF0ED82F),
]);
final gradientButtonGrey = LinearGradient(colors: [
  Colors.white.withOpacity(0.15),
  Colors.white.withOpacity(0.15),
]);
const gradientDisActiveTermsCategory =
    LinearGradient(colors: [itemColor, itemColor]);
const secondBackgroundColor = Color(0xFF282C3A);

const loadingColor = Color(0xFF1683E5);
